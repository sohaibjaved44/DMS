@model IEnumerable<DMS.Models.PYMT_TBL>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Pymt_id)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Pymt_Name)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Pymt_id)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Pymt_Name)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Pymt_id  }) |
            @Html.ActionLink("Details", "Details", new { id = item.Pymt_id }) |
            @Html.ActionLink("Delete", "Delete", new { id = item.Pymt_id })
        </td>
    </tr>
}

</table>
