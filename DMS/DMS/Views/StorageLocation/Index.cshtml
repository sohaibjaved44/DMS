@model IEnumerable<DMS.Models.STRG_LOC_TBL>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

@*<p>
    @Html.ActionLink("Create New", "Create")
</p>*@
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.STRG_ID)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.STRG_LOC)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.STRG_ID)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.STRG_LOC)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.STRG_ID }) |
            @Html.ActionLink("Details", "Details", new { id = item.STRG_ID })
        </td>
    </tr>
}

</table>
