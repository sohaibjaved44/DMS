@model IEnumerable<DMS.Models.DEPT_TBL>

@{
    ViewBag.Title = "Index";
}

<h2>Departments</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Dpt_id)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Dpt_Name)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Dpt_id)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Dpt_Name)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new {  id=item.Dpt_id}) |
            @Html.ActionLink("Details", "Details", new { id=item.Dpt_id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Dpt_id })
        </td>
    </tr>
}

</table>
