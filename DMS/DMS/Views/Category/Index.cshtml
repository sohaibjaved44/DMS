@model IEnumerable<DMS.Models.CTGRY_TBL>

@{
    ViewBag.Title = "Categories";
}

<h2>Categories</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            Category Id
        </th>
        <th>
            Category Name
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.CtgryId)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.CtgryName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new {  id=item.CtgryId }) |
            @Html.ActionLink("Details", "Details", new { id=item.CtgryId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.CtgryId })
        </td>
    </tr>
}

</table>
